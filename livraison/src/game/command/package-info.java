/**
 * Package contenant les commandes du patron Command.
 * <p>
 * Chaque commande encapsule une action utilisateur (création, suppression,
 * déplacement, redimensionnement, modification de forme) et supporte
 * l'annulation (undo) et le rétablissement (redo) via le {@link game.command.CommandManager}.
 * </p>
 *
 * @see game.command.Command
 * @see game.command.CommandManager
 */
package game.command;
