/**
 * Package contenant le contrôleur principal et l'énumération des types de formes.
 * <p>
 * Le {@link game.controller.MouseController} implémente le patron State
 * pour gérer les différents modes d'interaction (sélection, création,
 * modification, suppression).
 * </p>
 *
 * @see game.controller.MouseController
 * @see game.controller.state
 */
package game.controller;
